# 项目修改文档

> 更新日期：2026-04-05
> 项目版本：v1.2

---

## 一、版本历史

### v1.2 (当前版本) - 新增功能
### v1.1 - 代码问题修复

---

## 二、v1.2 新增功能

### 2.1 新增状态枚举类

| 文件 | 说明 |
|------|------|
| `entity/enums/ParseStatus.java` | 文献解析状态枚举 (PENDING/PROCESSING/SUCCESS/FAILED) |
| `entity/enums/TaskStatus.java` | 综述任务状态枚举 |
| `entity/enums/RelationType.java` | 文献关系类型枚举 (INHERIT/CONTRADICT/SUPPORT) |

### 2.2 观点传承图谱功能

**前端 (Workbench.vue)**
- 新增"观点传承图谱" Tab
- 添加 `analyzedRelations` 和 `isAnalyzing` 状态
- 添加 `startRelationAnalysis()` 方法调用后端

**后端**
- `PaperController`: 新增 `/analyze-relations` 接口
- `PaperService`: 新增 `analyzePaperRelations()` 方法
- `api.py`: 新增 `/analyze-relations` 接口调用大模型分析文献关系

**关系类型**
- `INHERIT` - 传承关系（后续研究在前人基础上发展）
- `CONTRADICT` - 矛盾关系（研究结论相互矛盾）
- `SUPPORT` - 支持关系（多个研究结论相互支持）

### 2.3 批量删除功能

- `PaperController`: 新增 `/batch-delete` 接口
- `PaperService`: 新增 `batchDeletePapers()` 方法
- 前端可选中多篇文献进行批量删除

### 2.4 Python 配置重构

**config.py 重构**
- 所有参数通过 `os.getenv()` 注入
- 每个配置项都有详细中文注释
- 新增 `http_client` 配置超时和代理

**api.py 新增接口**
- 新增 `/analyze-relations` 接口
- 所有接口使用配置注入参数

**新增 .env.example**
- 完整的 .env 配置示例文件
- 每个配置项都有详细注释

---

## 三、v1.1 问题修复

| 问题 | 文件 | 修复状态 |
|------|------|----------|
| 上传时错误调用解析 | PaperServiceImpl.java | ✅ 已修复 |
| 综述生成 URL 硬编码 | PaperServiceImpl.java | ✅ 已修复 |
| Thread 改 @Async | PaperServiceImpl.java | ✅ 已修复 |
| 多次 onMounted | Workbench.vue | ✅ 已修复 |

### 修复详情

#### 修复 1：上传时错误调用解析
- **问题**：上传文件后自动调用 Python 解析，但数据库保存操作在调用之后
- **修复**：移除自动解析逻辑，用户上传后仅保存记录，由用户手动触发解析

#### 修复 2：URL 硬编码
- **问题**：`generateOutline()` 硬编码 `http://127.0.0.1:8000`
- **修复**：改为使用 `pythonApiUrlConfig.replace("/parse", "/generate-outline")`

#### 修复 3：Thread 改 @Async
- **问题**：`triggerAiAnalysis()` 使用 `new Thread()` 实现异步
- **修复**：抽取 `executeAiAnalysisAsync()` 方法，使用 `@Async("taskExecutor")` 注解

#### 修复 4：多次 onMounted
- **问题**：Vue 组件中两次 `onMounted()` 导致执行顺序不确定
- **修复**：合并为一个 `onMounted()`

---

## 四、遗留问题

| 优先级 | 问题 | 说明 |
|--------|------|------|
| P2 | LiteratureReview 实体 | 已标记 @Deprecated，待确认无数据后可删除 |
| P3 | 日志规范化 | System.out/err 建议改用日志框架 |
| P3 | Token 安全 | 当前使用简单字符串替换，建议使用标准 JWT |

---

## 五、测试建议

### 5.1 业务逻辑测试
1. **上传功能**：上传 PDF，检查数据库和文件存储
2. **解析功能**：手动触发解析，检查状态流转
3. **综述生成**：选择多篇已解析文献，检查 Markdown 输出
4. **历史记录**：检查历史综述是否正确保存和展示
5. **关系分析**：点击"观点传承图谱"，检查文献关系展示
6. **批量删除**：选中多篇文献，测试批量删除

### 5.2 Docker 部署测试
```bash
docker compose build
docker compose up -d
docker compose ps
docker compose logs -f
```
